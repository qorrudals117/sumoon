<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>공지사항</h1>
    <table border="2">
        <tr>
            <td>번호</td>
            <td>제목</td>
            <td>제목2</td>
            <td>작성자</td>
            <td>작성일</td>
        </tr>

        <% rows.forEach((row,index) => {  %>
        <tr>
            <td><%= row.id%></td>
            <td><%= row.content%></td>
            <td><%= row.title%></td>
            <td><%= row.name%></td>
            <td><%= row.created%></td>
            <td>
                <input type="button" onclick="javascript:location.href='/updateMemo?id=<%=row.id%>'" value="수정">
                <input type="button" onclick="javascript:location.href='/deleteMemo?id=<%=row.id%>'" value="삭제">
            </td>
        </tr>
        <% }) %>
    </table>
    <input type="button" value="새 메모" onclick="javascript:location.href='/newnotice'">
</body>
</html>